[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd matrixjupiter && npm install"]

[phases.build]
cmds = ["cd matrixjupiter && npm run build"]

[start]
cmd = "cd matrixjupiter && npm start"
